{{- if .Values.web.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taas.fullname" . }}-web-nginx
  labels:
    {{- include "taas.labels" . | nindent 4 }}
data:
  default.conf: |
{{ include "taas.web.nginx.conf" . | nindent 4 }}
{{- end }}
